{
  "description": "charlesdr-dev-loop: route non-trivial edits to a codex lane instead of inline editing. Active only in repos with a .charles.toml.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/route-to-codex.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
