---
description: Check every codex lane and dependency is reachable
---

Run:

```bash
${CLAUDE_PLUGIN_ROOT}/scripts/doctor.sh
```

Report the output as-is. For anything FAIL, say which lane it kills and how to
fix it. Do not attempt repairs without asking.
